Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately? --Because of the types of files that are saved on the version control systems. 

How can you use the commands git log and git diff to view the history of files? --Git log command will show all the commits, and git diff will compare 2 of the commits to each other and show the changes

How might using version control make you more confident to make changes that could break something? --You can use git checkout to go back and find out what changes broke what you're working on.

Now that you have your workspace set up, what do you want to try using Git for? --Version control, esentially